check process wompt
	with pidfile <%= shared_path %>/pids/wompt.pid
	depends on wompt_auth
	start "/sbin/service wompt start"
	stop "/sbin/service wompt stop"
	if failed host 127.0.0.1 port 16999 protocol HTTP
		request /ok
		with timeout 5 seconds
		then restart
